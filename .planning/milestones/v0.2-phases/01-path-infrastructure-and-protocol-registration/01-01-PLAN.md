---
phase: 01-path-infrastructure-and-protocol-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rclone_filesystem/__init__.py
  - pyproject.toml
  - tests/test_path.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - TEST-04

must_haves:
  truths:
    - "All filesystem methods construct rclone paths through _make_rclone_path() -- no duplicated path construction remains"
    - "Paths with shell metacharacters raise ValueError before reaching rclone"
    - "Tilde (~) is allowed in paths for SFTP remotes"
    - "builtins.open is used explicitly inside the class, not bare open()"
    - "rclone-python dependency is >=0.1.24"
  artifacts:
    - path: "rclone_filesystem/__init__.py"
      provides: "_make_rclone_path(), _validate_path(), builtins.open usage"
      contains: "_make_rclone_path"
    - path: "pyproject.toml"
      provides: "Updated rclone-python dependency"
      contains: "rclone-python>=0.1.24"
    - path: "tests/test_path.py"
      provides: "Path helper and validation unit tests"
      min_lines: 40
  key_links:
    - from: "rclone_filesystem/__init__.py"
      to: "_make_rclone_path"
      via: "All methods (ls, open, cp_file, rm_file) call _make_rclone_path instead of inline path construction"
      pattern: "_make_rclone_path\\("
    - from: "rclone_filesystem/__init__.py"
      to: "_validate_path"
      via: "_make_rclone_path calls _validate_path internally"
      pattern: "_validate_path\\("
---

<objective>
Extract _make_rclone_path() helper with path validation, fix builtins.open shadowing, bump rclone-python dependency, and add path unit tests.

Purpose: Centralize all path construction into a single validated chokepoint, eliminating duplicated logic across 4 methods and adding shell injection protection.
Output: Updated __init__.py with path helper, updated pyproject.toml, new tests/test_path.py
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-path-infrastructure-and-protocol-registration/01-CONTEXT.md
@.planning/phases/01-path-infrastructure-and-protocol-registration/01-RESEARCH.md

<interfaces>
<!-- Current RCloneFileSystem class (97 lines) in rclone_filesystem/__init__.py -->
<!-- Path pattern repeated 4 times in ls, open, cp_file, rm_file: -->
```python
if path == "/":
    rclone_path = self._remote + ":"
else:
    rclone_path = self._remote + ":" + path.lstrip("/")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add path helper, validation, builtins.open fix, and dep bump</name>
  <files>rclone_filesystem/__init__.py, pyproject.toml, tests/test_path.py</files>
  <behavior>
    - _make_rclone_path("bucket/key") returns "remote:bucket/key"
    - _make_rclone_path("/bucket/key") returns "remote:bucket/key" (strips leading /)
    - _make_rclone_path("") returns "remote:" (empty = root)
    - _make_rclone_path("/") returns "remote:" (slash = root)
    - _make_rclone_path("dir/") returns "remote:dir/" (trailing slash preserved)
    - _make_rclone_path("a//b") returns "remote:a//b" (double slashes preserved)
    - _make_rclone_path("path;rm -rf /") raises ValueError with "invalid characters"
    - _make_rclone_path("path|cat") raises ValueError
    - _make_rclone_path("path$(cmd)") raises ValueError
    - _make_rclone_path("path`id`") raises ValueError
    - _make_rclone_path("path&bg") raises ValueError
    - _make_rclone_path("path\ninjection") raises ValueError
    - _make_rclone_path("~/documents/file.txt") returns "remote:~/documents/file.txt" (tilde allowed)
    - _make_rclone_path("normal/path/file.txt") returns "remote:normal/path/file.txt"
    - Error messages show bad characters found, NOT the full path (security)
    - All 4 existing methods (ls, open, cp_file, rm_file) use _make_rclone_path instead of inline construction
    - builtins.open is used inside the open() method instead of bare open()
  </behavior>
  <action>
1. Create `tests/test_path.py` with unit tests FIRST (RED phase):
   - Import RCloneFileSystem directly, instantiate with remote="testremote"
   - Test _make_rclone_path with: normal paths, empty string, "/", trailing slash, double slashes, paths with tilde
   - Test _validate_path rejects each metacharacter: ; | $ ` & ( ) { } < > \ \n \r
   - Test _validate_path allows tilde
   - Test error message contains "invalid characters" but NOT the full path
   - Use `pytest.mark.parametrize` for edge cases (TEST-04): double slash, trailing slash, empty string, root path
   - These tests do NOT need moto/rclone fixtures -- pure unit tests with direct class instantiation

2. Run tests -- they MUST fail (no _make_rclone_path yet).

3. Implement in `rclone_filesystem/__init__.py` (GREEN phase):
   - Add `import builtins` at top of file (INFRA-02)
   - Add `_INVALID_PATH_CHARS = frozenset(";|$` + "`" + '&(){}<>\\\n\r")` as class attribute
   - Add `@staticmethod _validate_path(path)`: check `set(path) & cls._INVALID_PATH_CHARS`, raise ValueError showing `sorted(bad)` chars only
   - Add `_make_rclone_path(self, path)`: call `self._validate_path(path)`, then `if path in ("", "/"): return self._remote + ":"` else `return self._remote + ":" + path.lstrip("/")`
   - Replace inline path construction in ls(), open(), cp_file(), rm_file() with `self._make_rclone_path(path)`
   - Replace bare `open(filename, mode)` calls in the open() method with `builtins.open(filename, mode)` (INFRA-02)
   - Path normalization rules per user decisions: strip leading / only, do NOT collapse double slashes, do NOT strip trailing slashes, do NOT detect/strip remote prefix

4. Update `pyproject.toml`: change `rclone-python>=0.1.21` to `rclone-python>=0.1.24` (INFRA-04)

5. Run `uv sync` to install updated dependency.

6. Run tests -- they MUST pass (GREEN phase).
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/rclone-filesystem && uv run pytest tests/test_path.py -x -v</automated>
  </verify>
  <done>
    - _make_rclone_path replaces all 4 inline path constructions
    - No f"{self._remote}:" pattern remains outside _make_rclone_path
    - Shell metacharacters in paths raise ValueError
    - Tilde is allowed
    - builtins.open used instead of bare open()
    - rclone-python>=0.1.24 in pyproject.toml
    - All path unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify existing tests still pass with refactored paths</name>
  <files>rclone_filesystem/__init__.py</files>
  <action>
Run the full existing test suite to confirm the path refactoring (replacing inline construction with _make_rclone_path) and builtins.open fix did not break any existing behavior. If any tests fail, diagnose and fix -- the refactoring should be behavior-preserving.

Note: Existing tests in tests/s3fs_compare/ require moto server and rclone binary. If they fail due to infrastructure issues (port conflicts, missing rclone), that is expected and will be addressed by Plan 03 (fixture hardening). Only code-related failures need fixing here.
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/rclone-filesystem && uv run pytest tests/ -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All existing tests pass (or only fail due to infrastructure issues that Plan 03 addresses)</done>
</task>

</tasks>

<verification>
- `grep -n "_make_rclone_path" rclone_filesystem/__init__.py` shows usage in ls, open, cp_file, rm_file
- `grep -n "self._remote.*:.*path" rclone_filesystem/__init__.py` returns NO matches outside _make_rclone_path
- `grep -n "builtins.open" rclone_filesystem/__init__.py` shows builtins.open usage
- `grep "rclone-python" pyproject.toml` shows >=0.1.24
- `uv run pytest tests/test_path.py -x -v` passes
</verification>

<success_criteria>
- _make_rclone_path() is the single chokepoint for all path construction
- Path validation blocks shell metacharacters
- builtins.open eliminates shadowing
- rclone-python dependency bumped to >=0.1.24
- Path edge case tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-path-infrastructure-and-protocol-registration/01-01-SUMMARY.md`
</output>
