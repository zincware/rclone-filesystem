---
phase: 01-path-infrastructure-and-protocol-registration
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - rclone_filesystem/__init__.py
  - pyproject.toml
  - tests/test_protocol.py
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - TEST-09

must_haves:
  truths:
    - "fsspec.filesystem('rclone', remote='myremote') returns an RCloneFileSystem instance"
    - "RCloneFileSystem._strip_protocol('rclone://myremote/bucket/key') returns 'bucket/key'"
    - "RCloneFileSystem._strip_protocol('rclone://myremote:bucket/key') returns 'bucket/key'"
    - "RCloneFileSystem._get_kwargs_from_urls('rclone://myremote/path') returns {'remote': 'myremote'}"
    - "_strip_protocol returns empty string for root paths"
    - "Plan 01's _make_rclone_path and _validate_path remain intact after modifications"
  artifacts:
    - path: "rclone_filesystem/__init__.py"
      provides: "protocol, root_marker, _strip_protocol, _get_kwargs_from_urls"
      contains: "protocol = \"rclone\""
    - path: "pyproject.toml"
      provides: "Entry point registration for fsspec"
      contains: "fsspec.specs"
    - path: "tests/test_protocol.py"
      provides: "Protocol registration and URL parsing tests"
      min_lines: 40
  key_links:
    - from: "pyproject.toml"
      to: "rclone_filesystem:RCloneFileSystem"
      via: "fsspec.specs entry point"
      pattern: "fsspec\\.specs"
    - from: "rclone_filesystem/__init__.py"
      to: "fsspec.AbstractFileSystem"
      via: "protocol class attribute enables discovery"
      pattern: "protocol = "
    - from: "rclone_filesystem/__init__.py"
      to: "_make_rclone_path"
      via: "Plan 01's path helper must survive Plan 02's modifications"
      pattern: "_make_rclone_path"
---

<objective>
Add fsspec protocol registration so `fsspec.filesystem("rclone")` discovers RCloneFileSystem, implement _strip_protocol and _get_kwargs_from_urls, and add protocol tests.

Purpose: Make rclone-filesystem discoverable through fsspec's standard protocol mechanism, enabling URL-based filesystem access.
Output: Updated __init__.py with protocol attributes and classmethods, updated pyproject.toml with entry point, new tests/test_protocol.py
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-path-infrastructure-and-protocol-registration/01-CONTEXT.md
@.planning/phases/01-path-infrastructure-and-protocol-registration/01-RESEARCH.md
@.planning/phases/01-path-infrastructure-and-protocol-registration/01-01-SUMMARY.md

<interfaces>
<!-- Current RCloneFileSystem in rclone_filesystem/__init__.py -->
<!-- NOTE: Plan 01 will have already added _make_rclone_path, _validate_path, -->
<!-- builtins.open fix, and rclone-python>=0.1.24 to these files. -->
```python
class RCloneFileSystem(AbstractFileSystem):
    """Rclone filesystem"""
    def __init__(self, remote: str):
        super().__init__(remote=remote)
        self._remote = remote
```

<!-- fsspec base class protocol mechanism -->
```python
# AbstractFileSystem has these class attributes:
# protocol = "abstract"  (override with "rclone")
# root_marker = ""       (empty string for root)
#
# And these classmethods:
# _strip_protocol(cls, path) -> str
# _get_kwargs_from_urls(path) -> dict
```

<!-- Entry point format for pyproject.toml -->
```toml
[project.entry-points."fsspec.specs"]
rclone = "rclone_filesystem:RCloneFileSystem"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add protocol registration, _strip_protocol, _get_kwargs_from_urls, and tests</name>
  <files>rclone_filesystem/__init__.py, pyproject.toml, tests/test_protocol.py</files>
  <behavior>
    - RCloneFileSystem.protocol == "rclone"
    - RCloneFileSystem.root_marker == ""
    - _strip_protocol("rclone://myremote/bucket/key") == "bucket/key"
    - _strip_protocol("rclone://myremote:bucket/key") == "bucket/key"
    - _strip_protocol("rclone://myremote") == ""
    - _strip_protocol("rclone://myremote/") == ""
    - _strip_protocol("rclone://myremote:") == ""
    - _strip_protocol("bucket/key") == "bucket/key" (passthrough)
    - _strip_protocol("") == ""
    - _strip_protocol(["rclone://r/a", "rclone://r/b"]) == ["a", "b"] (list input)
    - _get_kwargs_from_urls("rclone://myremote/path") == {"remote": "myremote"}
    - _get_kwargs_from_urls("rclone://myremote:path") == {"remote": "myremote"}
    - _get_kwargs_from_urls("rclone://myremote") == {"remote": "myremote"}
    - _get_kwargs_from_urls("bucket/key") == {} (no protocol prefix)
    - fsspec.filesystem("rclone", remote="s3-test") returns RCloneFileSystem instance
  </behavior>
  <action>
PREREQUISITE: Plan 01 must be complete. Read 01-01-SUMMARY.md to understand what was added.

1. Create `tests/test_protocol.py` with unit tests FIRST (RED phase):
   - Test `RCloneFileSystem.protocol == "rclone"`
   - Test `RCloneFileSystem.root_marker == ""`
   - Parametrized tests for `_strip_protocol` with all URL forms per behavior spec above
   - Tests for `_get_kwargs_from_urls` with all URL forms
   - Test for list input to `_strip_protocol`
   - Test `fsspec.filesystem("rclone", remote="s3-test")` returns RCloneFileSystem with correct _remote
   - These tests do NOT need moto/rclone fixtures -- pure unit tests

2. Run tests -- they MUST fail.

3. Implement in `rclone_filesystem/__init__.py` (GREEN phase):
   - Add `from fsspec.utils import stringify_path` import
   - Add class attributes: `protocol = "rclone"` and `root_marker = ""`
   - Add `_strip_protocol` classmethod:
     * Handle list input: `if isinstance(path, list): return [cls._strip_protocol(p) for p in path]`
     * `path = stringify_path(path)`
     * If starts with "rclone://", strip that prefix
     * If ":" in remaining: split on first ":", take second part (handles rclone://remote:path)
     * Elif "/" in remaining: split on first "/", take second part (handles rclone://remote/path)
     * Else: return "" (handles rclone://remote with no path)
     * Strip trailing "/" from result
     * Return `path or cls.root_marker`
   - Add `_get_kwargs_from_urls` staticmethod:
     * If starts with "rclone://", strip prefix
     * Extract remote name: split on ":" if present, else "/" if present, else entire string
     * Return `{"remote": remote}`
     * If no "rclone://" prefix, return `{}`

4. Add entry point in `pyproject.toml`:
   ```toml
   [project.entry-points."fsspec.specs"]
   rclone = "rclone_filesystem:RCloneFileSystem"
   ```

5. Run `uv sync` to rebuild package metadata (CRITICAL: entry points require reinstall).

6. Run tests -- they MUST pass (GREEN phase).

7. IMPORTANT: Verify Plan 01's changes survived by running:
   - `grep -n "_make_rclone_path" rclone_filesystem/__init__.py` -- must show usage in ls, open, cp_file, rm_file
   - `grep -n "_validate_path" rclone_filesystem/__init__.py` -- must show the validation function
   - `uv run pytest tests/test_path.py -x -q` -- Plan 01's tests must still pass
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/rclone-filesystem && uv sync && uv run pytest tests/test_protocol.py tests/test_path.py -x -v</automated>
  </verify>
  <done>
    - protocol = "rclone" set as class attribute
    - root_marker = "" set as class attribute
    - _strip_protocol handles all URL forms correctly
    - _get_kwargs_from_urls extracts remote kwarg
    - Entry point registered in pyproject.toml
    - fsspec.filesystem("rclone") discovers RCloneFileSystem
    - All protocol unit tests pass
    - Plan 01's _make_rclone_path and _validate_path still present and functional (grep confirms, test_path.py passes)
  </done>
</task>

</tasks>

<verification>
- `uv run python -c "import fsspec; fs = fsspec.filesystem('rclone', remote='test'); print(type(fs))"` prints RCloneFileSystem
- `uv run python -c "from rclone_filesystem import RCloneFileSystem; print(RCloneFileSystem._strip_protocol('rclone://myremote/bucket/key'))"` prints "bucket/key"
- `uv run pytest tests/test_protocol.py -x -v` passes
- `grep -c "_make_rclone_path" rclone_filesystem/__init__.py` returns >= 5 (definition + 4 call sites)
</verification>

<success_criteria>
- fsspec.filesystem("rclone") returns RCloneFileSystem
- _strip_protocol parses both URL forms correctly
- _get_kwargs_from_urls extracts remote
- Entry point registered and discoverable
- Plan 01's path infrastructure (_make_rclone_path, _validate_path) intact
</success_criteria>

<output>
After completion, create `.planning/phases/01-path-infrastructure-and-protocol-registration/01-02-SUMMARY.md`
</output>
